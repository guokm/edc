apiVersion: apps/v1
kind: Deployment
metadata:
  name: edc-data-plane
  namespace: edc
spec:
  replicas: 1
  selector:
    matchLabels:
      app: edc-data-plane
  template:
    metadata:
      labels:
        app: edc-data-plane
    spec:
      containers:
        - name: edc-data-plane
          image: edc-data-plane:local
          ports:
            - containerPort: 8182
          env:
            - name: EDC_DB_HOST
              value: "edc-mysql"
            - name: EDC_DB_PORT
              value: "3306"
            - name: EDC_DB_NAME
              value: "edc"
            - name: EDC_DB_USER
              value: "edc"
            - name: EDC_DB_PASSWORD
              value: "edc"
            - name: EDC_KAFKA_BOOTSTRAP
              value: "edc-kafka:9092"
            - name: EDC_KAFKA_GROUP
              value: "edc-data-plane"
---
apiVersion: v1
kind: Service
metadata:
  name: edc-data-plane
  namespace: edc
spec:
  selector:
    app: edc-data-plane
  ports:
    - port: 8182
      targetPort: 8182
